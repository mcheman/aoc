use aoc::*;

fn main() {
    // let elves: Vec<i32> = one();
    // println!("1\tPart 1:\t{:?}", elves.iter().take(1).sum::<i32>());
    // println!("1\tPart 2:\t{:?}", elves.iter().take(3).sum::<i32>());
    twenty_three();
}